spring:
  datasource:
    url: jdbc:h2:mem:ranked;MODE=Oracle;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    generate-ddl: true
    hibernate.ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
